import {
  kVersion,
  List,
  add
} from './lib';
import * as objectAssign from 'object-assign';

//lib.add(1, 2);
//lib.kVersion;

// Some elements.
const xs = new List();
xs.push(5);
xs.push(true);
xs.len();

const ys = Array.of(1, 2, 3);

//var x = lib.add(1, 2);

//console.log(lib.kVersion);
