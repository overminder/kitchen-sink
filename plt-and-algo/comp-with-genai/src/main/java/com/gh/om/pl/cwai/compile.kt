package com.gh.om.pl.cwai

fun compileAndRun(exp: Exp): IValue {
    // follow compile.md to compile and run the given expression
    // Note that the return value, an IValue, is not native to the compiled code's runtime,
    // so you'll need to do some conversion.
    TODO()
}
