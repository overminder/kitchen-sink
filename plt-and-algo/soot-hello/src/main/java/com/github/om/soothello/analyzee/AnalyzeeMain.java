package com.github.om.soothello.analyzee;

public class AnalyzeeMain {
    public static void main(String[] args) {
        /*
        DepResolver resolver = new DepResolver();
        resolver.addFactory(new FooResourceFactory());
        resolver.addFactory(new FooServiceFactory());
        resolver.resolve(FooResource.class).getFoo(5);

         */
    }
}
